  <!--Main Navigation-->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand mx-5 media" href="/home">
        <img class="align-self-center mr-3" src="/assets/images/logo.png" alt="logo"style="width:80px">
        <div class="d-inline-block media-body">
          <h1 class="mt-0" style="font-family: Roboto, sans-serif; font-weight: bold;">TP Consulting</h1>
        </div>      
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <% if(title=='Home'){%>
            <a class="nav-link active"href="/home">  
            <% } else { %>
            <a class="nav-link "href="/home" >
            <% } %>
            <i class="fas fa-sm fa-home"></i> Home</a>
          </li>
          <li class="nav-item">
            <% if(title=='About Us'){%>
              <a class="nav-link active"href="/about">  
              <% } else { %>
              <a class="nav-link "href="/about" >
              <% } %>
            <i class="fas fa-info"></i> About</a>
          </li>
          <li class="nav-item">
            <% if(title=='Projects'){%>
              <a class="nav-link active"href="/projects">  
              <% } else { %>
              <a class="nav-link "href="/projects" >
              <% } %>
            <i class="fab fa-elementor"></i> Projects</a>
          </li>
          <li class="nav-item">
            
            <% if(title=='Services'){%>
              <a class="nav-link active"href="/services">  
              <% } else { %>
              <a class="nav-link "href="/services" >
              <% } %>
            <i class="fas fa-cogs"></i> Services</a>
          </li>
          <li class="nav-item">
            <% if(title=='Contact'){%>
              <a class="nav-link active"href="/contact">  
              <% } else { %>
              <a class="nav-link "href="/contact" >
              <% } %>
            <i class="far fa-id-card"></i> Contact</a>
          </li>
        </ul>  
      </div>
    </div>
  </nav>